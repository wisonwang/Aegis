# build artifacts
/aegis
/datahub
/data/
*.db
*.db-journal
dist/

# local agent memory (project-private, do not publish)
.workbuddy/memory/

# local config overrides (keep placeholders out of VCS if desired)
# config.json

# temp / cleanup staging (build artifacts, logs, runtime db, throwaway scripts)
temp/

# macOS finder metadata
.DS_Store
*.DS_Store
