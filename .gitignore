# build artifacts
/aegis
/datahub
/data/
*.db
*.db-journal
dist/

# local agent memory (project-private, do not publish)
.workbuddy/memory/

# local TRAE project artifacts (generated/imported skills, local settings)
.trae/

# local config overrides (keep placeholders + secrets out of VCS)
conf/config.local.json

# temp / cleanup staging (build artifacts, logs, runtime db, throwaway scripts)
temp/

# macOS finder metadata
.DS_Store
*.DS_Store
