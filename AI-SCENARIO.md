# 企业 AI 应用建设场景：Aegis 的必要性与功能扩充规划

Database Proxy & Data Service Governance Platform — AI Scenario Deep-Dive

版本 v1.1 · 2026-07-28 · 配套文档：BLUEPRINT.md（总体蓝图 v1.0，§2.1 能力矩阵为权威状态来源）/ README.md（使用手册）

> **状态同步说明**：本文原始 v1.0（2026-07-23）的功能规划表大量标记为"规划"，而截至 2026-07-28 这些能力**已在代码中落地**。下表状态已与实际实现对齐；功能细节见 README.md 与各 ADR。

## 一、背景：AI 应用建设正在改变数据访问的基本假设

企业级 AI 应用（智能问数 ChatBI、知识库 RAG、Agent 工作流、研发/办公 Copilot）与传统应用最大的差异在于：**数据访问的发起者从「确定性的代码」变成了「概率性的模型」**。传统应用的每一条 SQL 都由工程师写死、经过评审；而 AI 应用中的 SQL 由 LLM 现场生成，其内容不可预测、不可穷举、且可被提示词注入操纵。

这意味着过去「应用持有数据库账号 + 代码内控权限」的模式在 AI 场景下完全失效——你无法对一个会自己写 SQL 的系统做代码评审。**治理必须从应用层下沉到数据访问层**，在 SQL 真正落到数据库之前，由一个独立、强制、可审计的中间层完成校验与改写。这正是 Aegis 的定位。

**一句话必要性：**没有治理代理层的 AI 应用，等价于给一个「不可控的实习生」发放了生产数据库 root 密码。Aegis 把它换成「带工牌、有权限边界、全程录像」的受控访问。

## 二、必要性论证：五大风险 × 平台对策（均已落地）

| 风险 | 说明 | 对策（状态） |
|------|------|--------------|
| 1 · 凭据失控 | 提示词注入可诱导 Agent 直连库即交出整库；凭据随代码/日志扩散 | **账号隔离** ✅：调用方只持 JWT / API Key，后端用受限服务账号 |
| 2 · 权限语义缺失 | DB 原生账号只有库/表粒度，表达不了业务边界 | **表/行/列三级治理** ✅：角色×数据源×表授权（默认拒绝）+ 行策略 `:attr` 注入 + 列脱敏 |
| 3 · 幻觉 SQL 无兜底 | NL2SQL 可能生成无 WHERE 的 DELETE、越权查询 | **SQL 解析级强制校验** ✅：解析→校验→重写→脱敏发生在引擎层，DDL 天然被拒 |
| 4 · 合规不可追溯 | 数安法/个保法/等保要求访问留痕 | **全量审计** ✅：`audit_logs` ok/denied/error 三态 + 渠道 + 会话串联 |
| 5 · 重复建设 | 每个 AI 项目各自接数据、各写权限 | **统一供给** ✅：DataAPI + MCP 共享同一治理与审计链路 |

## 三、典型 AI 场景映射

- **📊 智能问数 / ChatBI**：自然语言 → LLM 生成 SQL → **必须经 Aegis 强制治理后执行**。行策略保证「问自己能看的数」，列脱敏保证敏感字段不进对话上下文。
- **🤖 Agent 工作流**：多步骤 Agent 经 MCP 取数；API Key 映射低权身份，限流与行数上限防失控循环拖垮生产库。
- **📚 知识库 RAG 增强**：RAG 实时补充结构化数据走受治理的 DataAPI，避免向量库之外再开「裸连」通道。
- **💻 员工 Copilot**：按登录者身份（JWT attrs）取数，同一问题不同人得到各自权限内的答案——权限跟人走，而非跟应用走。

## 四、功能扩充规划（面向 AI 场景，状态已对齐代码）

### P0① AI 数据供给增强

| 功能 | 说明 | 状态 |
|------|------|------|
| MCP resources / prompts | 数据源目录、表结构语义卡片作为 resources 暴露；提供「如何安全查询」的 prompts 模板 | ✅ 已落地 |
| Schema 语义描述 | 表/列业务含义注释随 `describe_table` / `get_catalog` 下发，提升 NL2SQL 准确率 | ✅ 已落地 |
| NL2SQL 安全网关 | 「自然语言 → SQL → 治理校验 → 执行」端到端：LLM 生成只读 SQL 原样回灌受治理执行链路 | ✅ 已落地 |
| 语义指标层 | 管理员预定义核心指标 SQL 模板 + 类型化参数；Agent 运行指标而非现场编 SQL，根治口径漂移 | ✅ 已落地 |

### P0② AI 行为治理

| 功能 | 说明 | 状态 |
|------|------|------|
| 结果行数上限 | 按角色配置 max_rows，超限截断并审计标记 | ✅ 已落地 |
| 查询超时熔断 | 按数据源配置执行超时，慢查询主动取消 | ✅ 已落地 |
| 限流与配额 | 按用户/Key 配置 QPS，防 Agent 失控循环 | ✅ 已落地 |
| 高危语句拦截 | 无 WHERE 的 UPDATE/DELETE 直接拒绝；单表写执行前 SELECT COUNT(*) 预检影响行数，超 max_affected_rows 拒绝 | ✅ 已落地 |
| 查询成本预估 | 执行前 EXPLAIN 预估扫描行数 + 敏感度合成 low/medium/high 风险与可读告警 | ✅ 已落地（estimate_query） |

### P1③ 安全合规升级

| 功能 | 说明 | 状态 |
|------|------|------|
| 动态脱敏 | 掩码显示（phone/email/card/partial/hash/redact）+ 密钥化 Tokenization / FPE | ✅ 已落地 |
| 分级分类标签 | 列打 PII/敏感/内部/公开 标签，随语义目录与 MCP 卡片透出，并在 nl2sql 提示词标注敏感列规则 | ✅ 已落地 |
| 审计升级 | 关联 AI 会话 ID（一次对话多条查询串联）；异常行为告警（高频 denied / 批量导出 / 非工作时段 + Webhook） | ✅ 已落地 |
| 身份体系对接 | SSO / OIDC（Auth Code + PKCE + auto-provisioning + 组→角色映射）；LDAP / AD 三步绑定 + 组→角色映射；API Key 机制 | ✅ 已落地 |

### P1④ 平台化与生态

| 功能 | 说明 | 状态 |
|------|------|------|
| 多数据源扩展 | MySQL / PostgreSQL / SQLite 端到端；MongoDB / Elasticsearch / Trino 治理与 SQL 对齐 | ✅ 已落地 |
| 读写分离路由 | AI 查询默认路由只读副本，与交易流量物理隔离 | 🔲 规划 |
| 低代码 DataAPI | SQL 模板 + 参数定义一键发布为受治理 REST 接口 | 🔲 规划 |
| 云原生部署 | K8s Helm Chart、多副本（控制面迁 PostgreSQL）、Prometheus、OpenTelemetry | 🔲 规划 |

## 五、实施节奏建议（采用率优先，见 BLUEPRINT v1.0 §4）

1. **近期（阶段 A · 采用率验证）**：发布就绪（GitHub 描述/Topics/Release/Show&tell）+ README 叙事打磨 + 度量埋点 + 社区反馈闭环；门禁分级策略落地（ADR-002 A）；文档漂移清零。
2. **中期（阶段 B · 企业门槛）**：多租户工作区（ADR-001 Phase 0/1）、SIEM 审计转发、HA 控制面、审批流增强。
3. **远期（阶段 C · 平台化）**：读写分离路由、低代码 DataAPI、云原生部署、治理即代码、插件市场、向量检索。

**投入产出判断**：P0 两组能力（供给增强 + 行为治理）已全部落地，Aegis 已可支撑 ChatBI 与 Agent 工作流安全上生产；下一步重心从"加功能"转为"验证采用率 + 补齐企业门槛"。

Aegis · 企业 AI 应用建设场景专项规划 · v1.1 同步于 2026-07-28 · 与 BLUEPRINT.md v1.0 路线图对应：本文 ①② ≈ 阶段 3/4 前置项（均已落地）
